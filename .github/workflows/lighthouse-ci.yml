# Disabled placeholder; consolidated to lighthouse.yml
name: disabled-lighthouse-ci
on:
  push:
    branches:
      - never-run
  workflow_dispatch: {}
jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled placeholder workflow."
